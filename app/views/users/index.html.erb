<h1>Users</h1>

<table border="1" cellpadding="10">
  <tr>
    <th>id</th>
    <th>first name</th>
    <th>last name</th>
  </tr>
  <% @users.each do |u| %>
    <tr>
      <td><%= u.id %></td>
      <td><%= u.first_name %></td>
      <td><%= u.last_name %></td>
    </tr>
  <% end %>
</table>